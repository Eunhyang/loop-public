# Obsidian (전체 폴더 - 개인 설정, 충돌 방지)
.obsidian/

# Mac 시스템 파일
.DS_Store
.Trash-*

# Synology NAS 메타데이터
@eaDir/
*/@eaDir/

# IntelliJ IDEA
.idea/

# 임시 파일
*.tmp
*.temp
~$*

# Claude Code lock file (NAS sync 충돌 방지)
.claude-commit.lock

# === Auto-generated files ===
# _Graph_Index.md는 pre-commit hook에서 자동 생성/커밋됨

# Python 환경
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Poetry
poetry.lock
.env
.envrc

_exec_vault
api/oauth/keys/private.pem

# Build logs (자동 생성 로그 - 동기화 충돌 방지)
_build/ai_audit/
_build/run_log/
_build/build_logs/
_build/audit.log
_build/git-sync.log
_build/local-sync.log
_build/local-sync-stdout.log
_build/local-sync-stderr.log
_build/pending_reviews.json

# Attachments (tsk-dashboard-ux-v1-18: Task 첨부파일 - NAS에만 저장)
_attachments/

# Google OAuth DB (runtime, not tracked)
api/oauth/google_oauth.db
