# Dashboard Quick Search - Technical Specification

**Project**: dashboard-quick-search
**Version**: 0.1.0
**Last Updated**: 2025-12-25
**Status**: Planning

---

## 1. 개요

### 목적

Notion의 Command+P와 같은 전역 Quick Search 기능을 LOOP Dashboard에 추가.
키보드 단축키로 빠르게 Tasks, Projects, Tracks, Commands를 검색하고 실행할 수 있게 함.

### 핵심 기능

- **Cmd+K** (또는 Cmd+P) 단축키로 검색 모달 열기
- Tasks, Projects, Tracks, Conditions, Hypotheses 검색
- 커맨드 액션 (`>new task`, `>view kanban` 등)
- 키보드 네비게이션 (화살표 + Enter)
- 실시간 검색 결과 (debounced)

---

## 2. 아키텍처

### 기술 스택

- **Frontend**: Vanilla JavaScript (기존 대시보드와 일관성)
- **Styling**: CSS (기존 스타일 시스템 활용)
- **API**: 기존 `/api/search`, 캐시된 데이터 활용

### 파일 구조

```
_dashboard/
├── js/
│   └── components/
│       └── quick-search.js    # Quick Search 컴포넌트 (신규)
├── css/
│   └── quick-search.css       # Quick Search 스타일 (신규)
└── index.html                 # 모달 HTML 추가
```

### 데이터 흐름

```
Cmd+K 입력
    ↓
Quick Search 모달 열기
    ↓
검색어 입력 (debounce 200ms)
    ↓
검색 실행:
  1. 로컬 캐시 검색 (Tasks, Projects) - O(1)
  2. API 검색 (/api/search) - Vault 전체
    ↓
결과 렌더링 (카테고리별 그룹핑)
    ↓
키보드/마우스로 선택
    ↓
액션 실행 (열기, 생성, 뷰 전환 등)
```

---

## 3. UI 설계

### 모달 레이아웃

```
┌─────────────────────────────────────────────────────┐
│ 🔍 Search tasks, projects, commands...    [ESC]     │
├─────────────────────────────────────────────────────┤
│                                                     │
│ TASKS                                               │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 📋 tsk-003-01  온톨로지 스키마 작성             │ │
│ │ 📋 tsk-005-02  Day 4 데이터 투입               │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ PROJECTS                                            │
│ ┌─────────────────────────────────────────────────┐ │
│ │ 📁 prj-003  LoopOS V0 데이터 설계              │ │
│ │ 📁 prj-005  LoopOS 최소 선제 학습              │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ COMMANDS (> 입력 시)                                │
│ ┌─────────────────────────────────────────────────┐ │
│ │ ⚡ New Task                                     │ │
│ │ ⚡ New Project                                  │ │
│ │ 📊 View Kanban                                  │ │
│ │ 📅 View Calendar                                │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 검색 카테고리

| 카테고리 | 아이콘 | 데이터 소스 | 액션 |
|---------|-------|------------|------|
| Tasks | 📋 | AppState.tasks | Task Panel 열기 |
| Projects | 📁 | AppState.projects | Project Panel 열기 |
| Tracks | 📊 | AppState.tracks | Sidebar 선택 |
| Conditions | ⚠️ | AppState.conditions | Sidebar 선택 |
| Commands | ⚡ | 하드코딩 | 각 커맨드 실행 |

### 커맨드 목록 (> prefix)

| 커맨드 | 설명 | 액션 |
|-------|------|------|
| `>new task` | 새 Task 생성 | Task Modal 열기 |
| `>new project` | 새 Project 생성 | Project Modal 열기 |
| `>view kanban` | Kanban 뷰 | viewKanban 클릭 |
| `>view calendar` | Calendar 뷰 | viewCalendar 클릭 |
| `>view graph` | Graph 뷰 | viewGraph 클릭 |
| `>reload` | 캐시 리로드 | API.reloadCache() |

---

## 4. 키보드 단축키

| 키 | 액션 |
|----|------|
| `Cmd+K` / `Cmd+P` | 모달 열기 |
| `ESC` | 모달 닫기 |
| `↑` / `↓` | 결과 탐색 |
| `Enter` | 선택 실행 |
| `Tab` | 다음 카테고리로 이동 (optional) |

---

## 5. API 활용

### 로컬 캐시 검색 (우선)

```javascript
// AppState에서 직접 검색 (빠름)
const tasks = AppState.tasks.filter(t =>
  t.entity_name.toLowerCase().includes(query) ||
  t.entity_id.includes(query)
);
```

### API 검색 (Vault 전체)

```javascript
// 로컬 결과 부족 시 API 호출
const response = await API.get(`/search?q=${query}&limit=10`);
```

---

## 6. 성능 고려사항

- [x] Debounce 검색 (200ms)
- [x] 로컬 캐시 우선 검색
- [ ] 검색 결과 캐싱 (optional)
- [x] 최대 결과 수 제한 (카테고리당 5개)

---

## 7. 구현 순서

1. HTML 모달 구조 추가
2. CSS 스타일 작성
3. JavaScript 컴포넌트 작성
   - 모달 열기/닫기
   - 검색 로직
   - 결과 렌더링
   - 키보드 네비게이션
   - 액션 실행
4. app.js에 초기화 코드 추가
5. 테스트

---

**Version**: 0.1.0
**Status**: Living Document
