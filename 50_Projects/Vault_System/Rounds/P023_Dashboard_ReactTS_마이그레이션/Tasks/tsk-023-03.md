---
entity_type: Task
entity_id: "tsk-023-03"
entity_name: "Dashboard - 로그인 기능 구현"
created: 2026-01-08
updated: 2026-01-08
status: done
closed: 2026-01-08

# === 계층 ===
parent_id: "prj-023"
project_id: "prj-023"
aliases: ["tsk-023-03"]

# === 관계 ===
outgoing_relations: []
validates: []
validated_by: []

# === Task 전용 ===
assignee: "김은향"
start_date: 2026-01-08
due: 2026-01-08
priority: high
estimated_hours: null
actual_hours: null

# === Task 유형 (dev Task 연동용) ===
type: dev
target_project: loop

# === 분류 ===
tags: ["dashboard", "auth", "login"]
priority_flag: high
---

# Dashboard - 로그인 기능 구현

> Task ID: `tsk-023-03` | Project: `prj-023` | Status: done

## 목표

**완료 조건**:
1. 로그인 페이지 UI 완성 (이메일/비밀번호 폼)
2. MCP API `/oauth/dashboard-login` 연동
3. JWT 토큰 저장 및 관리
4. Route Guard 구현 (인증 없으면 /login 리다이렉트)
5. isAdmin 판단 로직 정상 동작

---

## 상세 내용

### 배경

Dashboard V2는 현재 bypass 토큰 (`loop_2024_kanban_secret`)으로 인증을 우회하고 있음.
실제 로그인 기능을 구현하여 사용자별 권한(admin/member) 구분 필요.

### 기존 API 확인

**엔드포인트**: `POST /oauth/dashboard-login`

**요청**:
```json
{
  "email": "admin@sosilab.com",
  "password": "admin123"
}
```

**응답**:
```json
{
  "access_token": "eyJ...",
  "token_type": "Bearer",
  "expires_in": 3600,
  "scope": "mcp:read mcp:exec",
  "role": "admin"
}
```

---

## 체크리스트

- [ ] Login 페이지 UI 구현 (이메일/비밀번호 폼)
- [ ] 로그인 API 호출 함수 (`features/auth/api.ts`)
- [ ] 토큰 저장 (`authStorage.setToken()`)
- [ ] Route Guard 컴포넌트 구현
- [ ] bypass 토큰 제거 (`services/http.ts`)
- [ ] 401 시 /login 리다이렉트 활성화
- [ ] isAdmin 판단 (JWT decode 또는 role 저장)

---

## Notes

### PRD

#### 📊 아키텍처 도식

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Login Feature Architecture                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ UI Layer (pages/Login/)                                                 │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  LoginPage ──→ LoginForm ──→ [email, password inputs]                  │ │
│  │      │              │                                                   │ │
│  │      ↓              ↓                                                   │ │
│  │  [useState]    handleSubmit() ──→ authApi.login()                      │ │
│  │  - email           │                                                    │ │
│  │  - password        ↓                                                    │ │
│  │  - isLoading   onSuccess: authStorage.setToken() + navigate('/kanban') │ │
│  │  - error       onError: setError()                                      │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│       │                                                                      │
│       ↓                                                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ Auth Layer (features/auth/)                                             │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  api.ts                    storage.ts                                   │ │
│  │  ├─ login(email, pw)       ├─ getToken()                               │ │
│  │  │    ↓                    ├─ setToken(token)                          │ │
│  │  │  POST /oauth/           ├─ getRole()                                │ │
│  │  │  dashboard-login        ├─ setRole(role)                            │ │
│  │  │    ↓                    └─ clearAll()                               │ │
│  │  │  { access_token,                                                     │ │
│  │  │    role, scope }                                                     │ │
│  │  └─ logout()                                                            │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│       │                                                                      │
│       ↓                                                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ Route Guard (routes/ProtectedRoute.tsx)                                 │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  ProtectedRoute                                                         │ │
│  │      │                                                                  │ │
│  │      ├─ authStorage.getToken() === null?                               │ │
│  │      │      ├─ YES → <Navigate to="/login" />                          │ │
│  │      │      └─ NO  → <Outlet /> (children)                             │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│       │                                                                      │
│       ↓                                                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ HTTP Layer (services/http.ts)                                           │ │
│  ├────────────────────────────────────────────────────────────────────────┤ │
│  │  httpClient.interceptors.request                                        │ │
│  │      └─ token ? set Authorization header (no bypass)                   │ │
│  │  httpClient.interceptors.response                                       │ │
│  │      └─ 401? → authStorage.clearAll() + redirect('/login')             │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│       │                                                                      │
│       ↓                                                                      │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ AppLayout - isAdmin = authStorage.getRole() === 'admin'                 │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 📋 프로젝트 컨텍스트

| 항목 | 값 |
|------|-----|
| Framework | React 19 + TypeScript |
| Build Tool | Vite 7 |
| Routing | React Router DOM 6 |
| HTTP Client | Axios |
| Styling | Tailwind CSS 4 |

#### 🎯 구현 범위

**주요 기능:**
1. 로그인 페이지 UI (이메일/비밀번호 폼)
2. 로그인 API 연동 (`POST /oauth/dashboard-login`)
3. JWT 토큰 + role 저장 (localStorage)
4. Route Guard (미인증 시 /login 리다이렉트)
5. 401 응답 시 자동 로그아웃
6. isAdmin 판단 (role 기반)

**파일 구조:**
```
src/
├── pages/Login/index.tsx              # ✏️ 수정: UI + 로직
├── features/auth/
│   ├── storage.ts                     # ✏️ 수정: role 저장 추가
│   └── api.ts                         # 🆕 신규: 로그인 API
├── routes/
│   ├── index.tsx                      # ✏️ 수정: ProtectedRoute 적용
│   └── ProtectedRoute.tsx             # 🆕 신규: Route Guard
├── services/http.ts                   # ✏️ 수정: bypass 제거, 401 처리
└── components/layout/AppLayout.tsx    # ✏️ 수정: role 기반 isAdmin
```

#### 📝 상세 요구사항

**1. Login 페이지 UI** (`pages/Login/index.tsx`)
- 이메일 입력 (type="email", 형식 검증)
- 비밀번호 입력 (type="password")
- 로그인 버튼 (로딩 중 비활성화)
- 에러 메시지 표시

**2. Auth API** (`features/auth/api.ts` - 신규)
```typescript
interface LoginResponse {
  access_token: string;
  token_type: string;
  expires_in: number;
  scope: string;
  role: 'admin' | 'exec' | 'member';
}
```

**3. Auth Storage 확장** (`features/auth/storage.ts`)
- getRole() / setRole() 추가
- clearAll() 추가

**4. Route Guard** (`routes/ProtectedRoute.tsx` - 신규)
- 토큰 없으면 /login 리다이렉트

**5. HTTP Interceptor** (`services/http.ts`)
- bypass 토큰 제거
- 401 시 로그아웃 + 리다이렉트

**6. AppLayout** (`components/layout/AppLayout.tsx`)
- `isAdmin = authStorage.getRole() === 'admin'`

#### ✅ 성공 기준
- [ ] 로그인 성공 시 토큰 저장 + /kanban 이동
- [ ] 로그인 실패 시 에러 메시지 표시
- [ ] 토큰 없이 / 접근 시 /login 리다이렉트
- [ ] 401 응답 시 자동 로그아웃
- [ ] admin role일 때 "New Program" 버튼 표시
- [ ] bypass 토큰 완전 제거

#### 🔍 테스트 정보
- API: `POST /oauth/dashboard-login`
- 계정: `admin@sosilab.com` / `admin123`

### 작업 로그

#### 2026-01-08 18:00
**개요**: Dashboard 로그인 기능 구현 완료. OAuth 연동, JWT 토큰 관리, Route Guard, 401 자동 로그아웃 모두 구현.

**변경사항**:
- 개발: Login 페이지 UI (이메일/비밀번호 폼)
- 개발: Auth API 모듈 (`features/auth/api.ts`)
- 수정: Auth Storage - role 관리 추가 (`getRole()`, `setRole()`, `clearAll()`)
- 개발: ProtectedRoute 컴포넌트 - Route Guard 구현
- 수정: HTTP Interceptor - bypass 토큰 제거, 401 자동 로그아웃
- 수정: AppLayout - role 기반 isAdmin 판단

**파일 변경**:
- `src/pages/Login/index.tsx` - 로그인 페이지 UI 및 로직 구현
  - 이메일/비밀번호 입력 폼
  - 로딩 상태 관리
  - 에러 메시지 표시
  - 로그인 성공 시 토큰 저장 + /kanban 이동
- `src/features/auth/api.ts` - 신규 생성
  - `LoginRequest`, `LoginResponse` 인터페이스 정의
  - `authApi.login()` - POST `/oauth/dashboard-login`
- `src/features/auth/storage.ts` - 확장
  - `getRole()`, `setRole()` 추가
  - `clearAll()` - 토큰 + role 일괄 삭제
  - `isAuthenticated()` - 인증 상태 확인
- `src/routes/ProtectedRoute.tsx` - 신규 생성
  - 토큰 없으면 `/login` 리다이렉트
  - 토큰 있으면 children 렌더링
- `src/routes/index.tsx` - 수정
  - `/login` 경로 추가
  - 모든 protected routes에 `<ProtectedRoute>` 적용
- `src/services/http.ts` - 수정
  - **bypass 토큰 완전 제거** (기존 `loop_2024_kanban_secret`)
  - Request interceptor: `authStorage.getToken()`으로 동적 토큰 설정
  - Response interceptor: 401 시 `authStorage.clearAll()` + `/login` 리다이렉트
- `src/components/layout/AppLayout.tsx` - 수정
  - `isAdmin = authStorage.getRole() === 'admin'`
  - Header에 isAdmin prop 전달

**결과**: ✅ 빌드 성공 (1.42s)
```
dist/assets/index-BJAUljTi.js   274.66 kB │ gzip:  80.61 kB
dist/assets/index-CF0rZrzC.js   367.25 kB │ gzip: 115.48 kB
✓ built in 1.42s
```

**검증 완료**:
- [x] 로그인 페이지 UI 완성 (이메일/비밀번호 폼)
- [x] MCP API `/oauth/dashboard-login` 연동
- [x] JWT 토큰 저장 및 관리
- [x] Route Guard 구현 (인증 없으면 /login 리다이렉트)
- [x] isAdmin 판단 로직 정상 동작 (role 기반)
- [x] 401 응답 시 자동 로그아웃
- [x] bypass 토큰 완전 제거

**다음 단계**:
- 실제 배포 환경에서 로그인 테스트 필요
- 필요 시 리프레시 토큰 구현 검토

---

## 참고 문서

- [[prj-023]] - 소속 Project
- `/oauth/dashboard-login` API 정의: `api/oauth/routes.py:375-434`

---

**Created**: 2026-01-08
**Assignee**: 김은향
**Due**: 2026-01-08
