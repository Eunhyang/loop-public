---
entity_type: Task
entity_id: "tsk-023-04"
entity_name: "DateFilters - 레거시 Quick Date Multi-Select 구현"
created: 2026-01-08
updated: 2026-01-08
status: done
closed: 2026-01-08

# === 계층 ===
parent_id: "prj-023"
project_id: "prj-023"
aliases: ["tsk-023-04"]

# === 관계 ===
outgoing_relations: []
validates: []
validated_by: []

# === Task 전용 ===
assignee: "김은향"
start_date: 2026-01-08
due: 2026-01-08
priority: high
estimated_hours: null
actual_hours: null

# === Task 유형 (dev Task 연동용) ===
type: dev
target_project: loop

# === 분류 ===
tags: ["dashboard-v2", "filter", "ui"]
priority_flag: high
---

# DateFilters - 레거시 Quick Date Multi-Select 구현

> Task ID: `tsk-023-04` | Project: `prj-023` | Status: done

## 목표

**완료 조건**:
1. 레거시 대시보드와 동일한 Quick Date Multi-Select UI 구현
2. Week/Month 모드 토글 동작
3. 주차/월 버튼 multi-select 동작
4. URL params (selectedWeeks, selectedMonths)와 연동

---

## 상세 내용

### 배경

레거시 대시보드의 Quick Date 필터:
- Mode Toggle: [W] [M] 버튼으로 Week/Month 선택
- Dynamic Buttons: 선택된 모드에 따라 주차/월 버튼들 동적 표시
- Multi-Select: 여러 주/월 선택 가능
- 표시 형식: `M1W2` (1월 2주차), ISO 형식 `2026-W02`

현재 V2 DateFilters.tsx:
- Quick Date: This Week / This Month 단순 토글만
- 레거시의 multi-select 기능 없음

### 작업 내용

DateFilters.tsx에 레거시 Quick Date 기능 추가:
1. Week/Month 모드 토글 UI
2. 주차/월 목록 동적 생성 (현재 날짜 기준 전후 N개)
3. Multi-select UI (클릭 시 toggle)
4. ISO Week 계산 유틸리티
5. 표시 형식 변환 (`2026-W02` → `M1W2`)

---

## 체크리스트

- [ ] Week/Month 모드 토글 버튼 추가
- [ ] 주차 목록 동적 생성 (getWeekOptions)
- [ ] 월 목록 동적 생성 (getMonthOptions)
- [ ] Multi-select 버튼 UI
- [ ] ISO Week 계산 유틸 함수
- [ ] 표시 형식 변환 함수 (M1W2 등)
- [ ] useCombinedFilters 연동
- [ ] 빌드 확인

---

## Notes

### PRD (Codex-Reviewed)

## 📊 아키텍처 도식

```
┌─────────────────────────────────────────────────────────────────┐
│              Quick Date Multi-Select Architecture                │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ UI Layer (DateFilters.tsx)                                │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  ModeToggle ──→ WeekButtons / MonthButtons               │   │
│  │  [W] [M]        [M12W4] [M1W1] [M1W2•] [M1W3]           │   │
│  │      │               │                                    │   │
│  │      ↓               ↓                                    │   │
│  │  mode state    toggle selectedWeeks/selectedMonths       │   │
│  └──────────────────────────────────────────────────────────┘   │
│       │                                                          │
│       ↓                                                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Utility Layer (dateUtils.ts)                              │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  getWeekOptions(count) ──→ [{key, label, isCurrent}]     │   │
│  │  getMonthOptions(count) ──→ [{key, label, isCurrent}]    │   │
│  │  getISOWeek(date) ──→ '2026-W02'                          │   │
│  │  formatWeekLabel('2026-W02') ──→ 'M1W2'                   │   │
│  └──────────────────────────────────────────────────────────┘   │
│       │                                                          │
│       ↓                                                          │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ State Layer (useCombinedFilters via URL params)           │   │
│  ├──────────────────────────────────────────────────────────┤   │
│  │  selectedWeeks: string[] ←── ?week=2026-W02&week=2026-W03│   │
│  │  selectedMonths: string[] ←── ?month=2026-01&month=2026-02│  │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 📋 프로젝트 컨텍스트

- **Framework**: React 19.2 + TypeScript 5.9
- **Build**: Vite 7.2
- **State**: URL params (react-router-dom) + localStorage
- **Styling**: Tailwind CSS 4.x
- **기존 Hooks**: `useCombinedFilters` (selectedWeeks/selectedMonths 이미 정의)

## 🎯 구현 범위

### 주요 기능
1. **Week/Month 모드 토글** - [W] [M] 버튼으로 표시 모드 전환
2. **주차 버튼 Multi-Select** - 현재 주 기준 ±2주 (총 5개), 클릭 시 배열 toggle
3. **월 버튼 Multi-Select** - 현재 월 기준 ±2개월 (총 5개), 클릭 시 배열 toggle
4. **ISO Week 유틸리티** - `2026-W02` 형식 계산 및 `M1W2` 라벨 변환

### 파일 구조
```
src/
├── utils/
│   └── dateUtils.ts             # 신규: ISO Week 계산, 라벨 변환
└── features/filters/components/FilterPanel/
    └── DateFilters.tsx          # 수정: Multi-Select UI 추가
```

## 📝 상세 요구사항

### 1. 유틸리티 함수 (src/utils/dateUtils.ts)

```typescript
// ISO Week 계산 (ISO 8601 표준 - 월요일 시작)
getISOWeek(date: Date): string  // → '2026-W02'

// 주차 라벨 변환 (월 인덱스 = ISO Week 시작일(월요일) 기준)
formatWeekLabel(isoWeek: string): string  // '2026-W02' → 'M1W2'

// 주차 옵션 생성 (현재 기준 -2 ~ +2, 시간순 오름차순)
getWeekOptions(count: number): { key: string; label: string; isCurrent: boolean }[]

// 월 옵션 생성 (현재 기준 -2 ~ +2, 시간순 오름차순)
getMonthOptions(count: number): { key: string; label: string; isCurrent: boolean }[]

// 현재 주/월 확인
isCurrentWeek(key: string): boolean
isCurrentMonth(key: string): boolean
```

### 2. DateFilters.tsx 수정

**로컬 상태**:
```typescript
const [mode, setMode] = useState<'week' | 'month'>('week');
```

**Mode Toggle UI**:
```tsx
<div className="qd-mode-buttons flex gap-1">
  <button onClick={() => setMode('week')} className={...}>W</button>
  <button onClick={() => setMode('month')} className={...}>M</button>
</div>
```

**Week/Month Buttons**:
```tsx
{mode === 'week' ? (
  getWeekOptions(5).map(({ key, label, isCurrent }) => (
    <button key={key} onClick={() => toggleWeek(key)} className={...}>
      {label}
    </button>
  ))
) : (
  getMonthOptions(5).map(({ key, label, isCurrent }) => (
    <button key={key} onClick={() => toggleMonth(key)} className={...}>
      {label}
    </button>
  ))
)}
```

**Toggle 핸들러**:
```typescript
const toggleWeek = (key: string) => {
  const next = filters.selectedWeeks.includes(key)
    ? filters.selectedWeeks.filter(k => k !== key)
    : [...filters.selectedWeeks, key];
  filters.setSelectedWeeks(next);
  // dateFilter 클리어 (충돌 방지)
  if (filters.dateFilter) filters.setDateFilter('');
};
```

### 3. 스타일링

| 상태 | 클래스 |
|------|--------|
| 기본 | `bg-zinc-800 text-gray-400 hover:bg-zinc-700` |
| 선택됨 | `bg-primary/80 text-white border-primary` |
| 현재 | `ring-2 ring-primary/50` |
| 현재+선택 | `bg-primary text-white ring-2 ring-white/30` |

### 4. 충돌 해결 로직

| 액션 | 결과 |
|------|------|
| Week/Month 버튼 선택 | `dateFilter` 클리어 |
| Custom Range 설정 | `selectedWeeks`, `selectedMonths` 클리어 |
| Clear Date Filters | 모든 날짜 필터 초기화 |

## 🔍 Codex 제안사항 (반영됨)

1. **라벨 로직 명확화**: 월 인덱스 = ISO Week 시작일(월요일) 기준
2. **옵션 정렬**: 시간순 오름차순 (과거 → 현재 → 미래)
3. **URL 파라미터 형식**: repeated params (`?week=2026-W01&week=2026-W02`)
4. **모드 전환 동작**: 선택 상태 유지 (모드별 독립적)
5. **기본 선택**: 없음 (빈 배열 = 필터 미적용)
6. **타임존**: 로컬 타임존 사용

## ✅ 성공 기준

- [ ] Week/Month 모드 토글 동작
- [ ] 주차 버튼 5개 표시 (현재 주 기준 ±2)
- [ ] 월 버튼 5개 표시 (현재 월 기준 ±2)
- [ ] Multi-select toggle 동작
- [ ] URL params 반영 (`?week=2026-W02`)
- [ ] 현재 주/월 `current` 스타일
- [ ] 선택된 버튼 `selected` 스타일
- [ ] 기존 Custom Range와 충돌 없음
- [ ] TypeScript 타입 에러 없음
- [ ] 빌드 성공

### Todo
- [x] PRD 작성 (prompt-enhancer)
- [x] Codex 리뷰
- [x] 구현
- [x] 테스트
- [x] Codex 최종 리뷰 및 버그 수정

### 작업 로그

**2026-01-08 - 구현 완료**

1. **파일 생성**
   - `src/utils/dateUtils.ts` - ISO Week 유틸리티 함수
     - `getISOWeek()`: ISO 8601 Week 계산
     - `formatWeekLabel()`: 'M1W2' 형식 라벨 변환
     - `getWeekOptions()`: 주차 옵션 생성 (현재 ±2주)
     - `getMonthOptions()`: 월 옵션 생성 (현재 ±2개월)

2. **파일 수정**
   - `src/features/filters/components/FilterPanel/DateFilters.tsx`
     - Week/Month 모드 토글 UI 추가
     - Multi-select 버튼 구현 (클릭 시 toggle)
     - URL params 연동 (selectedWeeks[], selectedMonths[])
     - Conflict resolution (week/month vs custom range)
   - `src/features/tasks/selectors.ts`
     - `getISOWeek()` import 및 사용으로 변경
     - 기존 non-ISO 주차 계산 로직 제거

3. **Codex 리뷰 및 버그 수정**
   - **Bug 1**: `getISOWeek()` W00 버그 수정
     - 문제: 1월 첫 주 이전 날짜가 `W00` 반환
     - 수정: 전년도 마지막 주로 정확히 계산
   - **Bug 2**: Task selector ISO Week 미사용
     - 문제: 기존 Sunday-based 주차 계산 사용
     - 수정: `getISOWeek()` 사용으로 변경
   - **Bug 3**: Legacy 'W' filter 불일치 (참고만)
     - `dateFilter='W'`는 여전히 Sunday-based
     - 신규 multi-select는 Monday-based ISO Week 사용

4. **빌드 결과**
   - ✅ TypeScript 컴파일 성공
   - ✅ Vite 빌드 성공 (1.43s)
   - ✅ 에러 없음

5. **구현 완료 기능**
   - [x] Week/Month 모드 토글 동작
   - [x] 주차 버튼 5개 표시 (현재 주 기준 ±2)
   - [x] 월 버튼 5개 표시 (현재 월 기준 ±2)
   - [x] Multi-select toggle 동작
   - [x] URL params 반영 (`?week=2026-W02`)
   - [x] 현재 주/월 `current` 스타일
   - [x] 선택된 버튼 `selected` 스타일
   - [x] 기존 Custom Range와 충돌 없음
   - [x] TypeScript 타입 에러 없음
   - [x] ISO Week 년 경계 edge case 처리

**2026-01-08 - Task 완료**

Task 상태를 `done`으로 업데이트했습니다.

**최종 완료 내용**:
- DateFilters Quick Date Multi-Select 기능 구현 완료
- ISO Week 유틸리티 (`dateUtils.ts`) 생성
- Week/Month 모드 토글 UI 구현
- Multi-select 버튼 및 URL params 연동
- Codex 최종 리뷰를 통한 버그 수정 완료 (W00 버그, Task selector ISO Week 적용)
- TypeScript 컴파일 및 Vite 빌드 성공

**변경된 파일**:
- `src/utils/dateUtils.ts` (신규)
- `src/features/filters/components/FilterPanel/DateFilters.tsx`
- `src/pages/Kanban/KanbanFilters.tsx`
- `src/features/filters/components/FilterPanel/index.tsx`
- `src/features/tasks/selectors.ts`

**최종 상태**: done


---

## 참고 문서

- [[prj-023]] - 소속 Project
- 레거시 HTML snippet (컨텍스트 참조)

---

**Created**: 2026-01-08
**Assignee**: 김은향
**Due**: 2026-01-08
