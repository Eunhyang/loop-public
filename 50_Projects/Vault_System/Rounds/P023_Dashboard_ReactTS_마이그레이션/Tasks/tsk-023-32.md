---
entity_type: Task
entity_id: "tsk-023-32"
entity_name: "Dashboard - Notion 스타일 ChipSelect 공통 모듈"
created: 2026-01-09
updated: 2026-01-09
closed: 2026-01-09
status: done

# === 계층 ===
parent_id: "prj-023"
project_id: "prj-023"
aliases: ["tsk-023-32"]

# === 관계 ===
outgoing_relations: []
validates: []
validated_by: []

# === Task 전용 ===
assignee: "김은향"
start_date: 2026-01-09
due: null
priority: high
estimated_hours: null
actual_hours: null

# === Task 유형 (dev Task 연동용) ===
type: dev
target_project: loop

# === 분류 ===
tags: ["dashboard", "react", "component", "ui", "notion-style"]
priority_flag: high
---

# Dashboard - Notion 스타일 ChipSelect 공통 모듈

> Task ID: `tsk-023-32` | Project: `prj-023` | Status: done

---

## 목표

Entity Drawer의 edit 모드에서 선택지가 6개 이하인 프로퍼티를 Notion 스타일 Chip 형태로 표시하는 공통 모듈 구현

**완료 조건**:
1. ChipSelect, ChipSelectExpand 공통 컴포넌트 구현
2. TaskForm, ProjectForm, ProgramForm, HypothesisForm에 적용
3. Core 멤버는 Chip으로, 나머지는 "더보기"로 접근 가능
4. 빌드 에러 없음

---

## Tech Spec

### 대상 필드

| 필드 | 선택지 | UI 패턴 |
|------|--------|---------|
| `status` | 5개 (todo/doing/done/hold/blocked) | Chip only |
| `priority` | 4개 (critical/high/medium/low) | Chip only |
| `type` | ~6개 (dev/bug/strategy/research/ops/meeting) | Chip only |
| `assignee` | Core 멤버 + 더보기 | Core Chip + Expand |
| `project` | 동적 | 6개↓ Chip, 6개↑ Chip + Expand |
| `owner` | Core 멤버 + 더보기 | Core Chip + Expand |

### 컴포넌트 구조

```
components/common/
├── ChipSelect.tsx          # 기본 Chip 선택 (enum용)
├── ChipSelectExpand.tsx    # 더보기 패턴 (member/project용)
└── chipColors.ts           # 색상 매핑 상수
```

### ChipSelect Props

```typescript
interface ChipOption {
  value: string;
  label: string;
  color?: { bg: string; text: string };
  icon?: string;
}

interface ChipSelectProps {
  options: ChipOption[];
  value: string | string[];
  onChange: (value: string | string[]) => void;
  multiple?: boolean;
  disabled?: boolean;
}
```

### ChipSelectExpand Props

```typescript
interface ChipSelectExpandProps extends Omit<ChipSelectProps, 'options'> {
  primaryOptions: ChipOption[];              // Chip으로 표시할 옵션
  allOptions: ChipOption[];                  // 전체 옵션 (더보기용)
  expandLabel?: string;                      // "더보기" 텍스트
}
```

### 색상 매핑 (Notion 스타일)

```typescript
// chipColors.ts
export const statusColors = {
  todo: { bg: 'bg-gray-100', text: 'text-gray-700' },
  doing: { bg: 'bg-blue-100', text: 'text-blue-700' },
  done: { bg: 'bg-green-100', text: 'text-green-700' },
  hold: { bg: 'bg-yellow-100', text: 'text-yellow-700' },
  blocked: { bg: 'bg-red-100', text: 'text-red-700' },
};

export const priorityColors = {
  critical: { bg: 'bg-purple-100', text: 'text-purple-700' },
  high: { bg: 'bg-red-100', text: 'text-red-700' },
  medium: { bg: 'bg-yellow-100', text: 'text-yellow-700' },
  low: { bg: 'bg-gray-100', text: 'text-gray-600' },
};
```

### Member 그룹핑 로직

```typescript
// Core 멤버 필터 (Chip으로 표시)
const CORE_ROLES = ['Founder', 'Cofounder', 'Member'];
const isCoreRole = (role: string) => CORE_ROLES.includes(role);

// API 응답: /api/members
// { id, name, icon, role, active }
```

### UI 디자인 (Notion 스타일)

```
Status:
┌──────────────────────────────────────────────────┐
│ [Todo] [Doing] [Done] [Hold] [Blocked]           │
│  gray   blue   green  yellow   red               │
│         ^^^^^ (선택됨 = 진한 배경 + 체크)         │
└──────────────────────────────────────────────────┘

Assignee:
┌──────────────────────────────────────────────────┐
│ [👩‍💻 김은향] [👨‍🔬 한명학] [+ 더보기]              │
│  ~~~~~~~~~~                                      │
│  (선택됨 = 테두리 + 체크)                         │
└──────────────────────────────────────────────────┘
```

---

## Todo

### Step 1: 공통 컴포넌트 생성
- [ ] `components/common/chipColors.ts` - 색상 상수 정의
- [ ] `components/common/ChipSelect.tsx` - 기본 Chip 선택 컴포넌트
- [ ] `components/common/ChipSelectExpand.tsx` - 더보기 패턴 컴포넌트

### Step 2: TaskForm 적용
- [ ] status: `<select>` → `<ChipSelect>`
- [ ] priority: `<select>` → `<ChipSelect>`
- [ ] type: 기존 Chip 유지 (이미 구현됨, 필요시 ChipSelect로 교체)
- [ ] assignee: `<select>` → `<ChipSelectExpand>` (Core + 더보기)
- [ ] project: `<select>` → `<ChipSelectExpand>` (6개 분기)

### Step 3: 다른 Form 적용
- [ ] ProjectForm: status, owner
- [ ] ProgramForm: status, owner
- [ ] HypothesisForm: status

### Step 4: 검증
- [ ] 빌드 확인 (`npm run build`)
- [ ] 각 Form에서 정상 동작 확인

---

## Codex Review Suggestions (2026-01-09)

| # | 이슈 | 해결 방안 |
|---|------|----------|
| 1 | Single vs Multi-select 혼재 | 필드별 명시: status/priority/type/project/owner = 단일, assignee = 단일 (현재) |
| 2 | Expand 시 선택된 non-primary 표시 | 선택된 항목은 항상 Chip으로 표시 (primary 아니어도) |
| 3 | Loading/Error 상태 | disabled 처리 + React Query isLoading/isError 활용 |
| 4 | 대용량 리스트 | 프로젝트 10개 이상 시 검색 기능 추가 (v2) |
| 5 | 접근성 | 기본 버튼 role 사용, 키보드 네비게이션은 v2 |
| 6 | 색상 fallback | `defaultColor = { bg: 'bg-gray-100', text: 'text-gray-700' }` |
| 7 | 정렬 기준 | Core 멤버 먼저, active=true 우선, 이름순 |
| 8 | API 에러 | React Query 에러 핸들링 기존 패턴 따름 |

---

## 참고

- 현재 TaskForm.tsx에서 type 필드만 Chip으로 구현됨 (317-333줄)
- Members API: `/api/members` - role 필드로 Core/Advisor/External 구분
- 색상은 Tailwind CSS 클래스 사용
- `dashboardData?.constants?.task?.statuses` 등에서 enum 값 가져옴

---

## 파일 변경 목록

| 작업 | 파일 | 변경 |
|------|------|------|
| 신규 | `components/common/chipColors.ts` | 색상 상수 |
| 신규 | `components/common/ChipSelect.tsx` | 기본 컴포넌트 |
| 신규 | `components/common/ChipSelectExpand.tsx` | 더보기 컴포넌트 |
| 수정 | `features/tasks/components/TaskForm.tsx` | select → Chip |
| 수정 | `features/projects/components/ProjectForm.tsx` | select → Chip |
| 수정 | `features/programs/components/ProgramForm.tsx` | select → Chip |
| 수정 | `features/strategy/components/HypothesisForm.tsx` | select → Chip |

---

## 참고 문서

- [[prj-023]] - 소속 Project
- `dashboard-v2/src/features/tasks/components/TaskForm.tsx` - 현재 Form 구현
- `dashboard-v2/src/contexts/UiContext.tsx` - Drawer 상태 관리
- `exec/40_People/members.yaml` - 멤버 역할 정보

---

## 작업 로그

### 2026-01-09
**개요**: Task 완료 - Notion 스타일 ChipSelect 공통 모듈 구현 완료

**구현 내역**:
- ChipSelect, ChipSelectExpand 공통 컴포넌트 구현
- chipColors.ts 색상 상수 정의 (status, priority, type 색상 매핑)
- TaskForm, ProjectForm, ProgramForm, HypothesisForm에 Chip 적용
- Core 멤버 Chip + 더보기 패턴 구현
- 빌드 확인 완료

**결과**:
- 3개 공통 컴포넌트 생성 (chipColors.ts, ChipSelect.tsx, ChipSelectExpand.tsx)
- 4개 Form 파일 수정 (TaskForm, ProjectForm, ProgramForm, HypothesisForm)
- Codex review suggestions 8개 항목 반영
- 빌드 에러 없음

**최종 상태**: done

---

**Created**: 2026-01-09
**Assignee**: 김은향
